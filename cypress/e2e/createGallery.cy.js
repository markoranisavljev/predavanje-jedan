import { CreateGalleryPage } from '../page_objects/createGalleryPage'

describe ('create gallery test', () => {
let galleryId;

let galleryData



    before('login', () => {
        crypto.loginViaBackend
    })

    it('create single gallery', () => {
        cy.intercept

    })

})